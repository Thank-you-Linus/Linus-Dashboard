# Environment variables for Linus Dashboard development
# Copy this file to .env and modify as needed

# Home Assistant configuration
PYTHONPATH="./custom_components"

# Development settings
DEBUG=true
HASS_CONFIG_DIR="./config"

# Optional: Set Home Assistant data directory
# HASS_DATA_DIR="./config"

# Optional: Set timezone
# TZ="Europe/Paris"

# Required for `make fake-house` (provisions the fake house test entities):
# a long-lived access token created in the HA UI under Profile > Security > Tokens,
# after the first-boot admin account is set up.
# HA_TOKEN=
